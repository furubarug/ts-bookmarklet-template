# bookmarklet-template 1.0.0

Template for bookmarklet development with TypeScript.

### Usage

- Cleanup
    - Modify `package.json`.
    - Update `LICENSE`.
- Build
    - Run `npm run build`.
    - Modify `build.js` if necessary to change `README.md`.
